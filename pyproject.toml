[build-system]
requires = ["uv_build>=0.4.0"]
build-backend = "uv_build"

[project]
name = "infogather"
version = "0.1.0"
description = "CLI tool for info garthering and management"
requires-python = ">=3.13"
dependencies = ["feedparser"]

[project.scripts]
inf = "infogather.cli:main"
